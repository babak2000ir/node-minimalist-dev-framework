{
  "name": "minore",
  "description": "Minimalist Node/React Development Framework",
  "version": "1.0.1",
  "scripts": {
    "build": "cd server && npm run build",
    "start": "cd server && cd dist && node server.cjs"
  }
}
